name: SEC EDGAR Filing Collection

on:
  schedule:
    - cron: '0 1 * * *' # 매일 10:00 KST (UTC 01:00)
  workflow_dispatch: # 수동 트리거

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: Collect SEC filings (chunked)
        env:
          API_URL: https://securitya.vercel.app/api/cron/sec-collect
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          echo "=== SEC EDGAR 공시 수집 시작 ==="
          for i in $(seq 1 60); do
            echo ""
            echo "--- Chunk $i ---"
            response=$(curl -s -w "\n%{http_code}" "$API_URL" \
              -H "Authorization: Bearer $CRON_SECRET" \
              --max-time 30)
            http_code=$(echo "$response" | tail -1)
            body=$(echo "$response" | sed '$d')

            echo "HTTP: $http_code"
            echo "$body" | jq '.' 2>/dev/null || echo "$body"

            if [ "$http_code" != "200" ]; then
              echo "Error: HTTP $http_code"
              exit 1
            fi

            status=$(echo "$body" | jq -r '.status' 2>/dev/null)
            if [ "$status" = "completed" ] || [ "$status" = "already_completed" ]; then
              echo ""
              echo "=== 수집 완료! ==="
              exit 0
            fi

            echo "Waiting 5s..."
            sleep 5
          done

          echo "Warning: 60 chunks processed but batch not completed"
          exit 1
